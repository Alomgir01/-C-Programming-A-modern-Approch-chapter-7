#include<stdio.h>
int main()
{
    /*char ch;
    int len=0;
    printf("Enter a message: ");
    ch=getchar();

    while(ch!='\n')
    {
        len++;
        ch=getchar();
    }
    printf("Your message was %d character long  ",len);
    */

    {
        int len=0,f;
        putchar(f)
;        while(getchar()!='\n')
            len++;
            printf("%d long.",len);
    }

}
